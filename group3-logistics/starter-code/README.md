# ParcelTrack v1.4.2

Internal parcel tracking service. (Original docs were on the old wiki — lost in the 2025 migration.)

Run: `pip install -r requirements.txt && uvicorn app.main:app --reload`
Test: `python -m pytest`

Known contacts: rider app team (LINE group), merchant integrations M-100 / M-113 / M-121.
